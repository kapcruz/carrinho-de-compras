version: '3'
services:
    php-fpm:
        container_name: php-fpm
        restart: always
        build:
            context: ./php-fpm
            dockerfile: Dockerfile
        networks:
            - code-network

networks:
    code-network:
        driver: bridge
